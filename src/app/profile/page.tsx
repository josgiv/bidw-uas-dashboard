import { ProfileClient } from "@/components/ProfileClient";

export default function ProfilePage() {
    return (
        <div className="w-full h-full bg-background pt-[64px]">
            <ProfileClient />
        </div>
    );
}
